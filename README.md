# T.A.C Interface Components

Reusable React components that recreate the supplied T.A.C dashboard screenshot.

## Production build

```bash
npm install
npm run build
```

The project is configured for Vercel static output through `vercel.json`.
